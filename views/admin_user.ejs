<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ระบบจัดการฐานข้อมูลผู้ใช้</title>
  <%- include('./font') %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2 class="text-center mb-4 prompt-medium">ระบบจัดการฐานข้อมูลผู้ใช้</h2>
    
    <table class="table table-hover table-striped table-bordered">
      <thead class="thead-dark prompt-medium">
        <tr class="prompt-light">
          <th>รหัสผู้ใช้</th>
          <th>ชื่อ</th>
          <th>อีเมล</th>
          <th>หน้าที่</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% data.forEach(function(user) { %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.role %></td>
            <td>
              <a href="/admin/user/edit/<%= user.id %>" class="btn btn-warning btn-sm">Edit</a>
              <!-- Add more action buttons like Delete if needed -->
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

  
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
