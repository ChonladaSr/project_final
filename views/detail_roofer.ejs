<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skill Deck</title>
    <%- include('./font') %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body>
    <%- include('./partials/header') %>

    <div class="max-full mx-auto p-4 bg-background rounded-lg shadow-md">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div class="relative">
            <img src="/uploads/<%= detail.photo1 %>" alt="Gallery Image 1" class="w-full h-auto rounded-lg" />
          </div>
          <div class="relative">
            <img src="/uploads/<%= detail.photo2 %>" alt="Gallery Image 2" class="w-full h-auto rounded-lg" />
          </div>
          <div class="relative">
            <img src="/uploads/<%= detail.photo3 %>" alt="Gallery Image 3" class="w-full h-auto rounded-lg" />
          </div>
        </div>
        <div class="mt-4">
            <div class="flex items-center">

              <img src="/uploads/<%= detail.profile_image %>" alt="Profile Picture" class="w-30 h-20 rounded mr-4" />
              <div>
                <h2 class="text-xl text-base prompt-medium"><%= detail.name %></h2>
                <!-- Contact information on the same line -->
                <div class="flex items-center mt-2 space-x-6">
                  <div class="flex items-center">
                    <img src="/path/to/phone-icon.png" alt="Phone Icon" class="w-4 h-4 mr-2" />
                    <span class="text-muted-foreground prompt-medium"><%= detail.phone %></span>
                  </div>
                  <div class="flex items-center">
                    <img src="/path/to/email-icon.png" alt="Email Icon" class="w-4 h-4 mr-2" />
                    <span class="text-muted-foreground prompt-medium"><%= detail.email %></span>
                  </div>
                </div>
              </div>
            </div>
            <p class="mt-4 text-muted-foreground prompt-medium">เกี่ยวกับช่าง</p>
            <button class="mt-4 bg-secondary text-secondary-foreground hover:bg-secondary/80 px-4 py-2 rounded-lg">ปรึกษาช่าง</button>
          </div>
        </div>
      




    <h1>Detail for ID: <%= detail.id %></h1>
<p>Team Name: <%= detail.name %></p>
<p>Task Name: <%= detail.job_type %></p>
<p>Job Scope: <%= detail.job_scope %></p>
<p>Status: <%= detail.status %></p>

<!-- ข้อมูลรีวิวและคอมเมนต์ -->
<div class="reviews">
  <h3>Reviews (Average Rating: <%= reviewData.average_rating %>, Total Reviews: <%= reviewData.review_count %>)</h3>
  
  <% comments.forEach(comment => { %>
    <div class="review">
      <p><strong><%= comment.customer_name %>:</strong> Rated <%= comment.rating %> stars</p>
      <p>Comment: <%= comment.comment %></p>
      <p>Date: <%= new Date(comment.created_at).toLocaleDateString() %></p>
    </div>
  <% }) %>
</div>

<a href="/users/roofer">Back to List</a>

</body>
</html>