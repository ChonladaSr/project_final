<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skill Deck</title>
    
</head>
<body>
    <h1>Detail for ID: <%= detail.id %></h1>
<p>Team Name: <%= detail.name %></p>
<p>Task Name: <%= detail.job_type %></p>
<p>Job Scope: <%= detail.job_scope %></p>
<p>Status: <%= detail.status %></p>

<!-- ข้อมูลรีวิวและคอมเมนต์ -->
<h2>Reviews</h2>
<ul>
  <% if (reviews.length > 0) { %>
    <% reviews.forEach(review => { %>
      <li>
        <strong>Customer:</strong> <%= review.customer_name %><br>
        <strong>Rating:</strong> <%= review.rating %>/5<br>
        <strong>Comment:</strong> <%= review.comment ? review.comment : 'No comment' %><br>
        <strong>Date:</strong> <%= new Date(review.created_at).toLocaleDateString() %>
      </li>
    <% }); %>
  <% } else { %>
    <li>No reviews yet.</li>
  <% } %>
</ul>

<a href="/users/roofer">Back to List</a>

</body>
</html>