<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <%- include('./font') %>

</head>

<body>

  <form action="/admin/user/edit/<%= user.id %>" method="POST">
    <div class="container pt-3">
      <div class="prompt-regular max-w-md mx-auto relative overflow-hidden z-10 bg-stone-100 p-8 rounded-lg shadow-md">
          <h2 class="text-2xl text-black prompt-bold mb-6 text-center">การแก้ไขข้อมูลผู้ใช้งาน</h2>

          <div class="mb-4">
              <label class="block text-sm prompt-medium text-gray-600" for="name">ชื่อ</label>
              <input
                  class="mt-1 p-3 w-full border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400"
                  type="text" id="name" name="name" value="<%= user.name %>" required>
          </div>

          <div class="mb-4">
              <label class="block text-sm prompt-medium text-gray-600" for="email">อีเมล</label>
              <input
                  class="mt-1 p-3 w-full border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400"
                  name="email" id="email" type="text" value="<%= user.email %>" required>
          </div>

          <div class="mb-4">
            <label class="block text-sm prompt-medium text-gray-600" for="email">สถานะ</label>
            <input
                class="mt-1 p-3 w-full border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400"
                name="role" id="role" type="text" value="<%= user.role %>" required>
        </div>
          <div class="flex justify-end space-x-4">
              <button
                  class="bg-gray-700 text-white px-4 py-2 prompt-bold rounded-lg hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-600"
                  type="submit">
                  บันทึก
              </button>

              <a href="/admin/user/delete/<%= user.id %>"
                  class="inline-flex items-center px-4 py-2 bg-red-600 hover:bg-red-700 text-white text-sm prompt-bold rounded-lg focus:outline-none focus:ring-2 focus:ring-red-600">
                  <svg stroke="currentColor" viewBox="0 0 24 24" fill="none" class="h-5 w-5 mr-2"
                      xmlns="http://www.w3.org/2000/svg">
                      <path
                          d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"
                          stroke-width="2" stroke-linejoin="round" stroke-linecap="round"></path>
                  </svg>
                  ลบ
              </a>
          </div>
      </div>
  </div>
  </form>
  </div>
  </div>



  <script src="https://cdn.tailwindcss.com"></script>

</body>

</html>