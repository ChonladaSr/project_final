<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ระบบจัดการฐานข้อมูลช่าง</title>
    <link rel="icon" href="https://cdn-icons-png.freepik.com/512/6144/6144388.png?ga=GA1.1.512366322.1722083948">

  <%- include('./font') %>
  <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
  <div class="p-6 bg-card">
    <h2 class="text-xl prompt-medium text-foreground text-center">ระบบจัดการฐานข้อมูลช่าง</h2>
    <table class="min-w-full divide-y divide-border mt-6 table-auto">
      <thead class="bg-background">
        <tr class="text-left">
          <th class="prompt-medium py-3 px-4">รหัสประจำตัว</th>
          <th class="prompt-medium py-3 px-4">ชื่อ</th>
          <th class="prompt-medium py-3 px-4">เบอร์โทรศัพท์</th>
          <th class="prompt-medium py-3 px-4">ประเภทของงาน</th>
          <th class="prompt-medium py-3 px-4">พื้นที่ให้บริการ</th>
          <th class="prompt-medium py-3 px-4">ช่วงราคา</th>
          <th class="prompt-medium py-3 px-4">ประสบการณ์</th>
          <th class="prompt-medium py-3 px-4">อีเมล</th>
          <th class="prompt-medium py-3 px-4"></th>
        </tr>
      </thead>
      <tbody class="bg-card divide-y divide-border">
        <% data.forEach(function(team) { %>
        <tr class="prompt-light hover:bg-gray-100">
          <td class="py-3 px-4"><%= team.id %></td>
          <td class="py-3 px-4"><%= team.name %></td>
          <td class="py-3 px-4"><%= team.phone %></td>
          <td class="py-3 px-4"><%= team.job_type %></td>
          <td class="py-3 px-4"><%= team.job_scope %></td>
          <td class="py-3 px-4"><%= team.range %></td>
          <td class="py-3 px-4"><%= team.experience %></td>
          <td class="py-3 px-4"><%= team.email %></td>
          <td class="py-3 px-4">
            <a href="/admin/team/edit/<%= team.id %>" class="text-blue-500 hover:underline">แก้ไข</a>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>


<!-- <body>
  <div class="container mt-5">
    <h2 class="text-center mb-4 prompt-medium">ระบบจัดการฐานข้อมูลช่าง</h2>
    
    <table class="table table-hover table-striped table-bordered">
      <thead class="thead-dark prompt-medium">
        <tr>
          <th>รหัสประจำตัว</th>
          <th>ชื่อ</th>
          <th>เบอร์โทรศัพท์</th>
          <th>ประเภทของงาน</th>
          <th>พื้นที่ให้บริการ</th>
          <th>ช่วงราคา</th>
          <th>ประสบการณ์</th>
          <th>อีเมล</th>
          <th></th>
   
        </tr>
      </thead>
      <tbody>
        <% data.forEach(function(team) { %>
          <tr class="prompt-light">
            <td><%= team.id %></td>
            <td><%= team.name %></td>
            <td><%= team.phone %></td>
            <td><%= team.job_type %></td>
            <td><%= team.job_scope %></td>
            <td><%= team.range %></td>
            <td><%= team.experience %></td>
            <td><%= team.email %></td>
          
            <td>
              <a href="/admin/team/edit/<%= team.id %>" class="btn btn-warning btn-sm">Edit</a>
              <form action="/admin/team/delete/<%= team.id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this team?');">Delete</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body> -->
</html>
